/**
 * Central icon module — everything comes from lucide-react so stroke weight
 * and style stay consistent across the site. Components take `className`
 * exactly like the old hand-rolled icons did.
 */
export {
  House as HomeIcon,
  Store as ShopIcon,
  ShoppingCart as CartIcon,
  Phone as PhoneIcon,
  Search as SearchIcon,
  Plus as PlusIcon,
  Minus as MinusIcon,
  Trash2 as TrashIcon,
  Info as InfoIcon,
  X as CloseIcon,
} from "lucide-react";
